package com.shopsphere.datagenerator.model

case class Address(
                    id: String,
                    buildingId: String,
                    unitNumber: String,
                    postalCode: String
                  )