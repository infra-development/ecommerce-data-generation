package com.shopsphere.datagenerator.model

case class Address(
                    id: String,
                    customerId: String,
                    buildingId: String,
                    unitNumber: String,
                    postalCode: String
                  )